import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFFFF4757);
  static const primaryDark = Color(0xFFCC2F3F);
  static const secondary = Color(0xFFFF6B35);
  static const accent = Color(0xFFFFD700);

  // Neutrals
  static const background = Color(0xFFF8F9FA);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF1F3F5);

  // Text
  static const textPrimary = Color(0xFF1A1A2E);
  static const textSecondary = Color(0xFF6B7280);
  static const textHint = Color(0xFFADB5BD);

  // Status
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);

  // Deal badge
  static const discountBadge = Color(0xFFFF4757);
  static const featuredBadge = Color(0xFFFFD700);

  // Gradient
  static const List<Color> primaryGradient = [
    Color(0xFFFF4757),
    Color(0xFFFF6B35),
  ];
}
